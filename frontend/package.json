{
  "name": "smartad-frontend",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev:tv": "pnpm --filter tv-display dev",
    "dev:mobile": "pnpm --filter mobile-web dev",
    "dev:admin": "pnpm --filter admin-dashboard dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=18"
  }
}
